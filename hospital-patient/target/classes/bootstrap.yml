server:
  port: 8082

spring:
  application:
    name: hospital-patient
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml

logging:
  level:
    com.hospital.patient.mapper: debug